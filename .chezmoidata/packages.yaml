install:
  linux-debian:
    install_cmd: sudo apt update \&\& sudo apt install -y -qq
    test_cmd: dpkg -s
    packages:
      - zsh
      - gh
      - git
      - bat
      - fd-find
      - ripgrep
